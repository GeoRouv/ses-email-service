# Database Configuration
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ses_email

# AWS Credentials (REQUIRED - obtain from AWS IAM)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# SES Configuration (Pre-configured for assessment)
SES_CONFIGURATION_SET=ses-assessment-tracking
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:148761646433:ses-assessment-events
VERIFIED_DOMAIN=candidate-test.kubbly.com

# Application Settings
APP_BASE_URL=http://localhost:8000
UNSUBSCRIBE_SECRET=change-me-in-production-use-long-random-string
FALLBACK_REDIRECT_URL=https://example.com

# Safety & Rate Limiting
ALLOWED_EMAIL_DOMAINS=kubbly.com
EMAIL_RATE_LIMIT_PER_HOUR=15

# Logging & Environment
LOG_LEVEL=INFO
ENVIRONMENT=development

# Notes:
# - Copy this file to .env and fill in your actual credentials
# - Never commit .env to version control
# - Generate a strong UNSUBSCRIBE_SECRET for production (e.g., openssl rand -hex 32)
# - APP_BASE_URL should be your deployed URL in production (for tracking links)
# - ALLOWED_EMAIL_DOMAINS limits recipient domains (comma-separated for multiple)
